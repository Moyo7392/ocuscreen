"use client";
import {useEffect,useState} from "react";import Link from "next/link";import {AppShell} from "@/components/AppShell";import {ResultView} from "@/components/ResultView";import {storage} from "@/services/storage";import type {StoredScreening} from "@/services/types";
export default function ResultsPage(){const[result,setResult]=useState<StoredScreening|null|undefined>(undefined);useEffect(()=>setResult(storage.current()),[]);return <AppShell>{result===undefined?<div/>:result?<ResultView result={result}/>:<div className="clinical-card mx-auto max-w-xl p-10 text-center"><p className="report-label">No active report</p><h1 className="serif-title mt-4 text-3xl">No result available</h1><p className="mt-3 text-sm leading-6 text-body">Complete a retinal screening before opening the report.</p><Link href="/upload" className="teal-button mt-7">Start a screening</Link></div>}</AppShell>}
